name: Sync chatgpt/chatgpt-heartbeat/ to adamlui/chatgpt-addons/chatgpt-heartbeat/greasemonkey/

on:
  push:
    branches: [ master, main ]
    paths:
      - 'chatgpt/chatgpt-heartbeat/**'

jobs:
  build:
    if: github.event.commits[0].committer.username != 'auto-sync-bot'
    runs-on: ubuntu-latest
    steps:

      - name: Checkout adamlui/userscripts
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.REPO_SYNC_PAT }}
          repository: adamlui/userscripts
          path: adamlui/userscripts

      - name: Checkout adamlui/chatgpt-addons
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.REPO_SYNC_PAT }}
          repository: adamlui/chatgpt-addons
          path: adamlui/chatgpt-addons

      - name: Sync chatgpt/chatgpt-heartbeat/ to adamlui/chatgpt-addons/chatgpt-heartbeat/greasemonkey/
        run: rsync -av --delete ${{ github.workspace }}/adamlui/userscripts/chatgpt/chatgpt-heartbeat/ ${{ github.workspace }}/adamlui/chatgpt-addons/chatgpt-heartbeat/greasemonkey/

      - name: Push to adamlui/chatgpt-addons
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          push_options: '--force'
          add_options: '--all'
          commit_user_email: auto-sync@userscripts.co
          commit_message: '${{ github.event.head_commit.message }} ↞ [auto-sync from `adamlui/userscripts`]'
          file_pattern: 'chatgpt-heartbeat/*'
          repository: adamlui/chatgpt-addons